# انتهاء الإعلانات وتنظيف الصور

## التنظيف التلقائي

يعمل المسار `/api/cleanup-expired` مرة يوميًا عبر Vercel Cron. يبحث عن الإعلانات التي انتهت صلاحيتها، ثم يحذف صورها الفعلية من Cloudinary باستخدام API موقّع (`image/destroy` مع `invalidate=true`). بعد نجاح الحذف يزيل روابط الصور من Firestore ويضع `mediaDeletedAt`. إذا فشل حذف صورة، تبقى بياناتها في `imageAssets` ويحاول التنظيف التالي مرة أخرى.

يجب إضافة متغير Production في Vercel:

```text
CRON_SECRET=<قيمة عشوائية طويلة>
```

تُرسل Vercel قيمة هذا المتغير تلقائيًا إلى مهمة Cron في ترويسة `Authorization`. لا تضع القيمة في GitHub ولا في الواجهة.

## التجديد

يظهر زر «تجديد الإعلان 20 يومًا» للمالك فقط خلال آخر يومين قبل `expiresAt`. يرسل الزر طلبًا إلى `/api/renew-ad`، حيث يتحقق الخادم من هوية المستخدم وملكيته للإعلان ومن نافذة التجديد قبل تمديد الصلاحية. لا تتغير الصور ولا حالة المراجعة؛ لذلك يبقى الإعلان خاضعًا لحالة الموافقة الحالية.

الإعلانات الجديدة تحفظ، بالإضافة إلى روابط العرض القديمة، قائمة `imageAssets` التي تحتوي على `publicId` المطلوب للحذف الفعلي من Cloudinary. كما يدعم التنظيف الإعلانات القديمة باستنتاج `publicId` من رابط Cloudinary عند الإمكان.
