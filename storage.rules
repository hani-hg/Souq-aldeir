rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Souq-aldeir uses signed Cloudinary uploads; Firebase Storage is intentionally disabled.
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
