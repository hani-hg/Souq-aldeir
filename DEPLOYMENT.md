# دليل نشر سوق دير الزور المفتوح

## متطلبات الإنتاج

يحتاج الخادم إلى إعدادات Firebase Admin وSMTP وCloudinary الموقّع للصور فقط. انسخ `.env.example` إلى إعدادات بيئة الاستضافة، ولا ترفع ملف `.env` إلى GitHub. يجب ضبط `APP_URL` على النطاق النهائي، و`CORS_ORIGIN` على النطاق نفسه، وإضافة متغيرات Firebase Admin وSMTP وCloudinary الثلاثة: `CLOUDINARY_CLOUD_NAME` و`CLOUDINARY_API_KEY` و`CLOUDINARY_API_SECRET`. لا توجد في الموقع أي بوابة دفع إلكتروني أو تحصيل رسوم.

## Cloudinary

لم يعد العميل يستخدم unsigned upload preset. نقطة `/api/cloudinary-sign` تتحقق من رمز Firebase ثم تصدر توقيعًا محدودًا بمجلد الصور. يجب تعطيل أو تقييد الـ unsigned preset القديم `souq_ads` في لوحة Cloudinary، ووضع حدود الحساب على حجم الصور وأنواعها ومعدل الرفع. لا تشارك `CLOUDINARY_API_SECRET` مع المتصفح أو داخل المستودع. رفع الفيديو معطّل من الواجهة والخادم وقواعد Firestore.

## Firebase

انشر `firestore.rules` بعد مراجعتها من Firebase Console أو Firebase CLI. أنشئ سجلًا عامًا `settings/stats` يحتوي على `userCount` و`totalVisits` فقط إذا أردت عرض الإحصاءات؛ لا تمنح الزوار صلاحية الكتابة إلى هذا السجل. يجب أن تُحدّث هذه القيم من عملية إدارية أو خادمية موثوقة.

قبل الإطلاق، احذف الإعلانات والحسابات التجريبية من مشروع Firebase الإنتاجي، وراجع الإعلانات الموجودة بحثًا عن عناوين اختبارية أو بيانات شخصية. لا تُنشئ إعلانات إنتاجية تحتوي على `userEmail`؛ بريد المستخدم ليس جزءًا من البيانات العامة.

## فحص ما بعد النشر

تحقق من أن `GET /api/health` يعيد الحالة 200 فقط بعد تهيئة Firebase Admin وSMTP وCloudinary. اختبر التسجيل، تسجيل الدخول، إنشاء إعلان، رفع الصور، رفض الملفات غير المسموحة، التعديل والحذف، طلب التمييز المجاني، المحادثة، الإبلاغ، طلب إعادة تعيين كلمة المرور، وروابط مشاركة الإعلان من متصفح زائر وحساب مستخدم وحساب مدير. تحقق من عدم وجود أي زر أو رابط أو طلب شبكة للدفع الإلكتروني.

اختبر أيضًا إدخال نص مثل `<img src=x onerror=alert(1)>` في عنوان ووصف إعلان. يجب أن يظهر النص حرفيًا دون تنفيذ. اختبر طلبات HTTP مخصصة تحاول إرسال `role` أو `views` أو `userEmail` أو `featured` عند إنشاء إعلان؛ يجب رفضها بقواعد Firestore.

## النشر على Vercel

اربط المستودع بمنصة Vercel، واضبط متغيرات البيئة للإنتاج، ثم نفّذ النشر. بعد النشر افتح النطاق النهائي، امسح Service Worker القديم أو حدّثه، وراقب `/api/health` وسجلات Vercel أول 24 ساعة. لا تعتبر الإطلاق مكتملًا قبل نجاح اختبار البريد الفعلي من وظيفة Vercel واختبار رفع مصادق عليه.
